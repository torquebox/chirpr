
- if @chirps.nil? || @chirps.size < 1
  %h2 Oh Noes!
  %p
    Looks like
    = params[:username]
    hasn't chirped yet!

- else
  %h2
    Chirps from
    = params[:username]

%ul.chirps
  - @chirps.each do |chirp|
    = partial :'partials/chirp', :locals=>{:chirp=>chirp}
