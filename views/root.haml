%h2 Recent Chirps
%ul.chirps
  - Chirp.latest.each do |chirp|
    %li 
      %span.messsage
        %blockquote
          = chirp.message
          %br
      %span.author
        Sent on
        = format_time(chirp.created_at)
        by
        %a{:href=>"/#{chirp.profile.name}"}
          = chirp.profile.name


